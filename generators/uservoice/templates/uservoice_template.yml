uservoice_options:
  # required
  key: <%= args.first %>
  host: <%= args.first %>.uservoice.com
  forum: <%= args.second %>
  showTab: true
  # optional
  alignment: left
  background_color: "#f00"
  text_color: white
  hover_color: "#06C"
  lang: en